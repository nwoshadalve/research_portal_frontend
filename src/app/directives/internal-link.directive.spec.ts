import { InternalLinkDirective } from './internal-link.directive';

describe('InternalLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new InternalLinkDirective();
    expect(directive).toBeTruthy();
  });
});
